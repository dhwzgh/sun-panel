name: Mirror Docker image to GHCR

on:
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull source image
        run: docker pull hslr/sun-panel:latest      #别人的镜像  hslr/sun-panel:latest

      - name: Re-tag
        run: |
          docker tag hslr/sun-panel:latest ghcr.io/${{ github.actor }}/hf-sunpanel:latest   #改成自己喜欢的名字hf-sunpanel

      - name: Push to GHCR
        run: docker push ghcr.io/${{ github.actor }}/hf-sunpanel:latest     #改成自己喜欢的名字hf-sunpanel

      - name: Show result
        run: |
          echo "Pushed: ghcr.io/${{ github.actor }}/hf-sunpanel:latest"    #改成自己喜欢的名字hf-sunpanel
